@model IEnumerable<GVB.Models.Cattle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CattleNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CattleNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CattleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CattleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CattleID })
        </td>
    </tr>
}

</table>
