@model IEnumerable<GVB.Models.CattleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CattleTypeDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CattleTypeDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CattleTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CattleTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CattleTypeID })
        </td>
    </tr>
}

</table>
