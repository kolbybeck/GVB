@model IEnumerable<GVB.Models.Feedlot>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.feedlotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.feedlotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.feedlotID })
        </td>
    </tr>
}

</table>
